version: '3.1'
services:

  db:
    image: mariadb
    container_name: mariadb_server
    restart: always
    env_file: .env
    environment:
      MARIADB_ROOT_PASSWORD: ${DATABASE_PASS}
      MARIADB_DATABASE: ${DATABASE_NAME}
    ports: ["3306:3306"]